(\d+)le?ve?ling(\w):
  query: (`unique `$1rlvl `-map `-jewel `-flask `$2 `-VeiloftheNight `-Voidhome)
(\d+)-(\d+)le?ve?ling(\w):
  query: (`unique `$1-$2rlvl `-map `-jewel `-flask `$3 `-VeiloftheNight `-Voidhome)
(\d+)le?ve?ling:
  query: (`unique `$1rlvl `-map `-jewel `-flask `-VeiloftheNight `-Voidhome)
(\d+)-(\d+)le?ve?ling:
  query: (`unique `$1-$2rlvl `-map `-jewel `-flask `-VeiloftheNight `-Voidhome)
le?ve?ling:
  query: (`unique `60rlvl `-map `-jewel `-flask `-VeiloftheNight `-Voidhome)
  
laby?(rinth)?uniq(ues)?:
  query: (`chitusneedle or `glitterdisc or `izarosdilemma or `izarosturmoil or `spineofthefirstclaimant or `vipersscales or `windsofchange or `xirgilscrank) `unid

Treasures:
  query: (`AegisAurora or `Crownofeyes or `HegemonyEra or `Kaomsheart or `kongorsundyingrage or `mjölner or `PerandusSignet or `shavronneswrappings or `soultaker or `tasteofhate or `voidbattery or `windripper) unid
  
(Perandus|Challenge)?LeagueUniq(ues)?:
    query: (`ThePerandusManor or `Seven-LeagueStep or `Trypanon  or `UmbilicusImmortalis or `Varunastra or `Xirgil'sCrank or `Zerphi'sLastBreath) 